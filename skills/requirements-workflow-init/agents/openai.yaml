interface:
  display_name: "Requirements Workflow Init"
  short_description: "Create reqs workflow starter files"
  default_prompt: "Use $requirements-workflow-init to initialize the requirements workflow in this repo."

policy:
  allow_implicit_invocation: false
