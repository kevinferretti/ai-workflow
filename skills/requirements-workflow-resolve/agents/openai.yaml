interface:
  display_name: "Requirements Workflow Resolve"
  short_description: "Resolve or route interpretation questions"
  default_prompt: "Use $requirements-workflow-resolve to resolve the next open requirement question."

policy:
  allow_implicit_invocation: false
